{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:core": "lerna exec --scope @json-viewer/core -- yarn build",
    "dev:vue": "lerna exec --scope @json-viewer/vue -- yarn dev"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.8"
  },
  "type": "module",
  "useWorkspaces": true,
  "command": {
    "publish": {
      "registry": "https://registry.npmjs.org/"
    }
  }
}
